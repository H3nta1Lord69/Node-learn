const nombre = 'Deadpool';
const real = 'Wade Wilson';

// Using backticks you could use literal templates to add variables in a log using `${}`

const template = `${nombre} ${real}`;

console.log(template);

const html = `
    <h1>Hello there</h1>
    <p>Que pex</p>
`;

console.log(html);