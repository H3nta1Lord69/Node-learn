class Searchs {

    history = [];

    constructor () {
        //TODO: Read db if exists
    }

    async city( place = '' ) {
        //TODO: http petition
        console.log(place);

        return []; // Return places that have any coincidence with the input
    }

}

module.exports = Searchs;